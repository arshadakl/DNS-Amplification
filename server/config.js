export const config = {
    PORT: 53,                
    RESPONSE_SIZE: 512,       // Amplified response size in bytes
    LOG_REQUESTS: true,       // Enable/Disable logging of incoming queries
  };
  