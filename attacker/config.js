export const config = {
    VICTIM_IP: '192.168.1.100',      // Spoofed victim's IP address
    DNS_SERVER_IP: '192.168.1.101',  // Target DNS server's IP address
    DNS_PORT: 53,                    // Target DNS server's port
    SPOOF_RATE: 100,                 // Spoofed requests per second
    THREADS: 4,                      // Number of threads (adjust for your system)
  };
  